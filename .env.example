# Heritage Hub Nepal - Environment Variables

# For Netlify Frontend Deployment
# Set these in Netlify Dashboard: Site settings > Build & deploy > Environment
VITE_API_URL=https://your-backend-api.com

# For Local Development (optional)
# Leave empty to use relative paths (works when backend and frontend are on same origin)
# VITE_API_URL=

# Backend Configuration (used when running full stack locally)
DATABASE_URL=postgresql://user:password@host:port/database
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=heritage_hub_nepal
PGHOST=localhost
